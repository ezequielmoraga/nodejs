
//Codigo no funcional
//Promise.all genera 1 promesa, en base a un array de promesas.

function funcion(frame) {
    return Promise.all( ([array de promesas]).then (valores) =>{
        console.log(valores);
    }    );
  }